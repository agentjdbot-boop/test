name: R D Techno APK Build Staging

on:
  workflow_dispatch:

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Staging source update; final build workflow will be restored after upload."
